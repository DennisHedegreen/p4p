# tracker

Legacy product-track placeholder from the earlier broader P4P app concept.

It described a central registry plus tablet/customer apps.

That is not the canonical `v0.1` build target anymore.

## Current rule

If you are building the active alpha, work in:
- `registry/`
- `demo-node/`
- `client/`

Only return to this folder if the broader product track is explicitly reactivated.
